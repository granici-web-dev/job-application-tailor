# Sub-shape PR-{{pr_number}} — {{pr_title}}

> Part of the from-trd series. Master: `{{master_shape_path}}` · Spec: `{{trd_path}}`.

## Goal

<one sentence — what this PR makes true>

## Approach

<2-4 sentences — the strategy at the right altitude>

## Files

- `path` — <what changes>

## Schema / API changes

<empty if none, otherwise: column adds, route signatures, breaking changes>

## Test plan

<which tests / checks prove this PR works; e2e must run in scope per the pre-PR gate>

## Tradeoffs / alternatives considered

<2-3 alternatives rejected and why>

## Open questions

<anything needed from the user before craft>

## Done when

- [ ] `{{command_prefix}}rigorous critique` run on the staged diff; block-level findings fixed pre-commit
- [ ] this sub-shape cited in the commit body
- [ ] no open tasks remain in scope (since last `git checkout -b`)
